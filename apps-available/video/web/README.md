Put the built web-app-video here as `video/` (so `video/manifest.json` exists).
Download the release zip and unzip it here, or build the app and copy its
`dist/` to `video/`. No routes.yaml: this app has no backend.
