Put the built web-app-audio here as `audio/` (so `audio/manifest.json` exists).
Download the release zip and unzip it here, or build the app and copy its
`dist/` to `audio/`. No routes.yaml: this app has no backend.
