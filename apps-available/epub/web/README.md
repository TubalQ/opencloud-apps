Put the built web-app-epub here as `epub/` (so `epub/manifest.json` exists).
Download the release zip and unzip it here, or build the app and copy its
`dist/` to `epub/`. No routes.yaml: this app has no backend.
